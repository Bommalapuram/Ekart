apiVersion: v1
kind: Service
metadata:
  name: ekart-service
spec:
  selector:
    app: ekart
  ports:
    - protocol: TCP
      port: 80          # Service port
      targetPort: 8070  # Pod port
      nodePort: 32000   # Direct IP tho access cheyali ante idi use avthundi
  type: NodePort
